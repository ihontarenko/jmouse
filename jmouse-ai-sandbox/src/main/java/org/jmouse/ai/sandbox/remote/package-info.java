/**
 * The third entry point: this application reaching another one, and the caller not being able to tell.
 *
 * <p>Same workshop, same guards, same transcript. What is added is a second instance whose tools arrive
 * in the first one's catalogue — and every scenario is about the consequence of that being one catalogue
 * rather than two.
 *
 * <p>Run it with {@code mvn exec:java -Dexec.mainClass=org.jmouse.ai.sandbox.remote.RemoteToolsSandbox}.
 */
package org.jmouse.ai.sandbox.remote;
